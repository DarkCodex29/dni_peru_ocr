# Example

A minimal Flutter example wiring the camera, Google ML Kit Latin TextRecognizer,
and `OcrFieldExtractor` will live here. For now, see the [Quick start](../README.md#quick-start)
in the package README.
